# Local development only. pub reads this and never publishes it.
#
# dartvel_core no longer depends on dartvel_shelf: the wire types moved here
# and the HTTP client is built from this package's own rust/ crate. Nothing to
# override.
