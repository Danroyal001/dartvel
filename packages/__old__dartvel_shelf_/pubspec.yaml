name: dartvel_shelf
description: Shelf-like server for dartvel, WinterCG-first, Rust core (HTTP/1.0/1.1/2/3, WS, SSE, gzip).
version: 1.0.0
environment:
  sdk: ">=3.3.0 <4.0.0"
dependencies:
  ffi: ^2.1.0
  meta: ^1.11.0
  shelf: ^1.4.2
dev_dependencies:
  ffigen: ^12.0.0
  lints: ^5.0.0
  test: ^1.25.0
executables:
  build_rust: tool/build_rust.dart
