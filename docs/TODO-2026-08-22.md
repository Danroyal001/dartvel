# Dartvel — what is left, Saturday 22 August 2026

Written after a day that found more than it fixed, which is the right ratio
this early but means the list below is longer than it was this morning.

**The honest headline: 1,101 tests, all on Linux x86_64, zero integration
tests, and one of sixteen targets verified by actually running.** Everything
else marked ✅ means "an artifact was produced and inspected". That gap is the
real work, and most of this list is downstream of it.

Work top to bottom. Delete a line when it is done.

---

## 0. Disk — do this first, it is at 98%

762 MB free on a 32 GB volume. This blocks everything else, and it is mostly
self-inflicted: cargo was installed today to stop iterating on Rust through CI
logs, which was the right call and cost 2.5 GB.

| Item | Size | Regenerable |
| --- | --- | --- |
| `.toolchains/flutter` | 1.5 GB | yes, slow |
| `packages/dartvel_shelf/rust/target` | 1.5 GB | yes — `cargo clean` |
| `.toolchains/rustup` | 595 MB | yes |
| `.toolchains/cargo` | 395 MB | yes |
| `dartvel/.git` | 135 MB | no |
| `/workspaces/*.bundle` | 95 MB | **no — pre-rewrite backups** |

- [ ] `cargo clean` in `packages/dartvel_shelf/rust` reclaims ~1.5 GB and costs
      one rebuild. Do this whenever disk gets tight; it is the cheapest 1.5 GB
      available.
- [ ] Decide about the three `.bundle` files. They are the only copy of
      pre-history-rewrite state. The rewrite is verified (every tree hash
      identical, remotes clean), so they can go — but that is a one-way door,
      so it is a decision rather than a cleanup.
- [ ] Do **not** touch `~/.claude/projects` or editor state to free space.

---

## 1. Windows — three bugs found today, two fixed, one open

This target had never once reached the Dartvel CLI. It now does.

- [x] **The hang.** Not Flutter — the native asset hook. `cargo build` was
      unbounded, so a cold-cache compile of ~200 crates was indistinguishable
      from a wedge, and it ran to whatever cap it was given (257 and 226
      minutes on two earlier attempts). The hook runs before `dart run` reaches
      a `main`, which is why the CLI's `--build-timeout` could never help.
      Bounded and streamed; Rust now compiles in **3m22s**.
- [x] **ffigen could not find the pub cache.** Revealed only once the hang was
      gone. A child process inherited an environment with neither `PUB_CACHE`
      nor `LOCALAPPDATA`. Fixed with `hookChildEnvironment`.
- [ ] **Visual Studio detection is a false negative.** Dartvel probes for `cl`
      on `PATH`; `cl` is not on `PATH` outside a Developer Command Prompt, so
      a runner with the full C++ workload installed is reported as missing it
      and the build **skips**. Flutter itself uses `vswhere.exe` at
      `%ProgramFiles(x86)%\Microsoft Visual Studio\Installer\vswhere.exe`.
      Fix it the way the others were fixed: pure function first
      (`visualStudioFoundIn(vswhereOutput)`), then wire a probe onto
      `ToolRequirement`.
- [ ] Re-run and get an actual artifact. Nothing here is proven until
      `build/windows/x64/runner/Release` exists and has been listed.

---

## 2. macOS — still undiagnosed, and genuinely different from Windows

- [ ] **Find the hang.** Its native hook finishes in **11.71 seconds**, so it
      is not the Windows bug. `🔨 Building for macos...` prints and then
      nothing for 41 minutes. No `xcodebuild` among the orphaned processes at
      cleanup, only two `dartvm` — so Flutter starts, blocks before printing
      anything, and never reaches Xcode. `_isPlatformAvailable` is pure and
      cannot be it, and both child pipes are drained, so silence means the
      child produced nothing rather than that output was lost.
- [ ] The next probe should capture what the process is *doing*, not only that
      it stopped: `sample` or `spindump` on the wedged `dartvm`, uploaded as a
      diagnostics artifact.

**Do not conflate this with Windows.** They looked identical from the outside
and were not the same bug at all; assuming they were is what would waste the
next attempt.

---

## 3. The verification gap — the item everything else rests on

- [ ] **Integration tests. There are none.** Not one test launches an
      application. The only `integration_test` matches in the tree are inside
      the CLI's own `test_command.dart` — a command that *would* run them.
- [ ] Start with Linux, the one target already proven to run under Xvfb. A
      test that launches the example, asserts a frame rendered, and exits is
      worth more than any number of additional widget tests.
- [ ] Then per target, in the order the hosts are available: web, android,
      linux, then the CI-only ones.

**Until this exists, "✅ Builds" is doing more work than it has earned**, and
the v1 bar of builds-and-launches-and-passes is met by exactly one target.

---

## 4. Terminal rendering — designed, one step built

Specified today in `NEW_SPEC.md` § Terminal Rendering. `Draft`/`Partial`.

- [x] `-cli` / `-tui` suffix resolution, refusal on platforms with no terminal,
      and `resolveRenderBackends` — the rule that no build links a backend it
      did not ask for. 10 tests.
- [ ] Read `dartvel.terminal` from `pubspec.yaml` and feed it to
      `resolveRenderBackends`. Pure Dart, testable now.
- [ ] `DV.Platform.surface`, `DV.Platform.terminal.size` (a signal, so a
      resized terminal drives the same reactive path a resized window does),
      and `.graphics`.
- [ ] Launch negotiation: GUI by default, `--tui` to start in the terminal, and
      the no-display prompt. Pure Dart, testable without a TTY.
- [ ] **`dartvel_flt` fork** of [`jiahaog/flt`](https://github.com/jiahaog/flt)
      — Rust, Flutter's Custom Embedder API, Kitty graphics with an ANSI
      fallback. Upstream runs apps in development and does not produce
      distributable binaries; supplying that is the fork's job. Do this last —
      it is the part with an engine pin and the most ways to go wrong.

`android-cli` stays out, deliberately. Termux is Android userland: bionic, not
glibc, and no `/lib/ld-linux-aarch64.so.1`, so a `linux-cli` binary does not
fail partway through — it does not start. A test asserts the refusal.

---

## 5. Outbound HTTP — HTTP/2 done, HTTP/3 left

- [x] Protocol negotiation, ordered fallback chains, RFC 8297 early hints,
      composite transport, and a native HTTP/2 client on the `h2` crate
      **verified against a live server** (real `cf-ray`, `server: cloudflare`).
- [x] APNS (17 tests) and Web Push (15 tests), both unblocked by the above.
- [ ] **HTTP/3** via `h3` + `h3-quinn` + `quinn`.
- [ ] **Neither APNS nor Web Push has ever touched a real server.** Both suites
      use fake transports; what is verified is the request *shape* against
      Apple's and RFC 8291's documented requirements, not that Apple accepts
      it. A single live smoke test against each, behind `#[ignore]`-style
      opt-in, would close this.

Two gaps are upstream, not ours, and are recorded in `docs/http-transport.md`:
early hints over HTTP/3 (the `h3` crate has no informational path) and
*sending* 103 from Axum (hyper #2426, open since 2021). Both fork-shaped.

---

## 6. The blocked embedders

- [ ] **`mix` fork.** `dartvel_flutter` depends on `mix: ^2.0.0`, which needs
      Dart ≥ 3.11. This is the actual blocker for webOS and Sony eLinux, and it
      is ours, not the vendors'.
- [ ] **Re-pin `dartvel_webos` and `dartvel_elinux`** to a bleeding-edge
      Flutter and build the engines from source. Both forks currently inherit
      the vendor's pin, which is a choice not yet un-made rather than a limit
      imposed on us.
- [ ] **`dartvel_fuchsia` needs a build-only entry point.**
      `build_flutter_app.sh` delegates to `build_and_run_example.sh`, which
      tries to run the result on a device through `tools/ffx`. CI has no device.
      Everything up to that point now works: it builds the Flutter bundle and
      stages the app into the workspace.

---

## 7. Spec decisions waiting on you

- [ ] **Q28 — ratify or reject.** Audit done today,
      `docs/q28-contract-audit.md`. 14 sections are `Contract` while only
      `Partial`. Not a rule violation — the rule files permit a frozen-but-
      unbuilt contract deliberately. Recommendation: narrow `Contract` to UI,
      Routing and Models, and move the other 14 to `Draft`. A label can be
      raised once something depends on it; it cannot be lowered without
      breaking a promise. **Nothing was edited to produce that recommendation.**
- [ ] **Q12** — rewrite the Pages section so `DartvelPage` classes are the
      blessed path, with the private-input rule applying only where an input
      generates a public twin.
- [ ] **Device-fleet pricing** — cutting it was a recommendation you never
      ratified. Marked provisional in the private business-model doc.

Current label spread: 22 Contract/Shipped, 14 Contract/Partial, 15
Draft/Partial, 5 Draft/Designed, plus 20 narrative sections that correctly
carry none.

---

## 8. Not started

- [ ] **Six proposal items** from `docs/proposals/2026-08-platform-additions.md`
      (#5, #4, #7, #2, #6, #9) — drafted, never built.
- [ ] **User-facing documentation does not exist.** There is `README.md` and
      there is `NEW_SPEC.md`, and nothing between them: no getting started, no
      guides, no API reference. For a framework aiming at WordPress-scale
      adoption this is the largest non-code gap on the list.
- [ ] **Enterprise repo is empty of code**, correctly — nothing enterprise
      exists yet to move into it.

---

## What changed today, for context

CI now runs the test suites at all — it did not before, so a green commit meant
nothing had been checked. It found a stale `DVStaticPaths` export on its first
run. The spec-status check was path-filtered in a way that could never catch
the thing it exists to catch, and now is not. Diagnostics upload as artifacts
because the Actions logs endpoint throttles reads for long stretches, always
when a failing build most needs reading.

`@DVStaticPaths` was folded into `@DVModel(publicPathsResolver:)`. `computed`
was removed in favour of operating on signals directly. Git history was
rewritten across eight repositories to remove AI attribution, verified
byte-identical.

Two rules were written into the eleven agent files today because they kept
being lost: no AI attribution in commits, and test-driven development —
failing test first, and never adjust a test so existing code passes.
